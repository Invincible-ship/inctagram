export enum LanguageIds {
  RU = 'ru',
  EN = 'en',
}

export enum Namespaces {
  DEFAULT = 'translation',
  SIGNUP = 'signup',
  SIGNOUT = 'signout',
  SIGNIN = 'signin',
}

export type LanguageParams = { lng: LanguageIds }
